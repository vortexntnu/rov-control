# Uranus DP

### Future ideas
For the controller node (may be applicable to others): Make a class for each controller to be implemented. Easy switching. Same can go for state estimation.

### Dependencies
Depends on the eigen library which can be downloaded as the package libeigen3-dev with apt-get (or any other package manager)
